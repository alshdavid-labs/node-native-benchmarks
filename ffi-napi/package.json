{
  "name": "example-ffi-napi",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "install": "rm -rf target && cargo build --release"
  },
  "dependencies": {
    "ffi-napi": "^4.0.3"
  }
}
